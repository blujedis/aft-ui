import {
	// filledBg,
	// filledText,
	// outlinedText,
	// outlinedRing,
	// ghostBgHover,
	// ghostText,
	// softBg,
	// softText,
} from '$lib/constants/states';
import { mergeConfigs } from '$lib/theme/utils';

export const button = {
	// text: mergeConfigs(outlinedText, {}),
	// filled: mergeConfigs(filledBg, filledText),
	// outlined: mergeConfigs(outlinedText, outlinedRing, {
	// 	$base: 'ring-1 ring-inset'
	// }),
	// ghost: mergeConfigs(ghostText, ghostBgHover),
	// soft: mergeConfigs(softText, softBg)
};
