import { placeholder } from '../../constants';
export const paginationDetail = {
    filled: {
        $base: 'text-inherit'
    },
    flushed: {
        ...placeholder,
        $base: 'text-inherit'
    },
    ghost: {}
};
