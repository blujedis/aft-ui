import { placeholder } from '../../constants';
import { mergeConfigs } from '../../theme';
export const grid = {
    outlined: {
        ...placeholder
    },
    filled: {
        ...placeholder
    },
    glass: {
        ...placeholder
    },
    ghost: {
        ...placeholder
    }
};
