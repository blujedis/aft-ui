import { variantOutlined, variantFilled, variantFlushed } from '../Button/config';
const defaultSelect = {
    ...variantOutlined
};
export const select = {
    default: {
        ...defaultSelect
    },
    outlined: {
        ...defaultSelect
    },
    filled: {
        ...variantFilled
    },
    flushed: {
        ...variantFlushed
    }
};
