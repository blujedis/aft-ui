const component = {
    focused: true,
    hovered: true,
    rounded: 'none',
    shadowed: 'none',
    size: 'md',
    theme: 'default',
    transitioned: true
};
const defaults = {
    component
};
export default defaults;
