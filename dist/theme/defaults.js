const component = {
    focused: true,
    // rounded: 'sm',
    // shadowed: undefined,
    size: 'md',
    theme: 'light',
    transitioned: true
};
const defaults = {
    component
};
export default defaults;
