const component = {
    focused: true,
    hovered: true,
    rounded: 'sm',
    shadowed: 'none',
    size: 'md',
    theme: 'frame',
    transitioned: true
};
const defaults = {
    component
};
export default defaults;
